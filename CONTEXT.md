# CONTEXT.md

## Purpose

This file stores evolving project context for future sessions.

It should contain the current state of the project, recent decisions, relevant assumptions, and short notes that help resume work without depending on session memory.

## Repository Snapshot

- Project: `TransitOps`
- Reference date: 2026-05-31
- Repository status: local backend baseline established, transport/vehicle/driver CRUD now operational on PostgreSQL with numeric enum persistence, transport list filters/pagination available for demo use, explicit transport assignment and lifecycle transitions available, shipment-event creation/history with actor traceability is now operational, first-admin bootstrap/login/JWT endpoint protection are now in place, admin-only user management is now operational, the local Docker startup path is standardized through `.env.example`, compose validation, and a dedicated local verification guide, a dedicated Postman/Newman smoke flow exists against the live Docker stack with hard cleanup of runtime and seed data, GitHub Actions workflows exist for CI, deploy, Terraform, and rollback, the AWS target topology, cloud conventions, Terraform remote-state strategy, security/cost posture, reliability runbooks, and operations runbooks are documented, Terraform includes the full `dev` runtime stack, Sprint 6-8 AWS validations recreated and destroyed the cost-bearing `dev` environment in account `661000947340` (`Pablo`, alias/profile `aws-pey-v1`), and Sprint 9 has added final requirements traceability, evidence index, and verification/rehearsal documentation.
- Solution: `TransitOps.slnx`
- Main projects:
  - `TransitOps.Api`
  - `TransitOps.Tests`

## Current Understanding

- The project goal is to build a transport management backend with a deliberately small functional scope and stronger focus on cloud architecture and DevOps practices.
- The current repository already includes the API bootstrap, EF Core migrations-managed PostgreSQL setup, local Docker composition, and planning documentation.
- The API now includes EF Core PostgreSQL persistence wiring with a `TransitOpsDbContext`, entity configurations, and a first baseline migration under `TransitOps.Api/Infrastructure/Persistence/Migrations`, which is now the canonical schema source.
- Enum-like fields (`transport.status`, `shipment_event.event_type`, `app_user.user_role`) now use `smallint` columns with explicit check constraints instead of native PostgreSQL enums, preserving the .NET enums in code while avoiding provider-specific runtime friction.
- The API readiness endpoint now validates real PostgreSQL connectivity through `TransitOpsDbContext.Database.CanConnectAsync()`.
- The functional MVP surface is implemented enough to support the cloud phase without broadening scope or reworking the backend baseline.
- Transport create, update, logical delete, filtered listing, basic pagination, explicit vehicle+driver assignment, lifecycle transitions, shipment-event registration/history, first-admin bootstrap, login, JWT issuance, protected endpoint access, and admin-only user management are now implemented on top of `TransitOpsDbContext`, using explicit validation and active-reference conflict checks, so the main operational slice has coherent list/detail/create/update/delete/assign/transition/event/auth/user behavior against PostgreSQL.
- Vehicle and driver CRUD are now implemented on top of `TransitOpsDbContext`, including explicit validation, active-row conflict checks on business identifiers, and logical delete behavior consistent with transport soft deletion.
- Planning is now anchored in `docs/Requirements.md` for scope and acceptance, and `docs/Roadmap.md` for sprint execution from the real repository state as of March 29, 2026.
- The roadmap now uses one-week sprints instead of day-by-day planning, with each sprint defining a dominant phase, mandatory scope, explicit artifacts, and a strict definition of done.
- The requirements specification now explicitly defines user management, role permissions for `admin` and `operator`, main operational flows, and stronger acceptance detail.
- The roadmap is now structured from March 30, 2026 through May 31, 2026 as weekly sprints, with Sprint 1 intentionally compressing the full local-MVP closure plus cloud design baseline so the rest of the calendar can focus on AWS platform, delivery, observability, reliability, security, and final defense quality.
- The current planning target end date is 2026-05-31, with cloud credibility now treated as the main differentiator of the project.
- The AWS target runtime is now explicitly fixed as Route53 + ACM + ALB + ECS Fargate + RDS PostgreSQL + ECR + CloudWatch, with private ECS/RDS networking, OIDC-based GitHub Actions access, and externalized runtime configuration.
- Shared cloud conventions are now documented in the same cloud reference as the target topology and remote-state bootstrap path, so Terraform and delivery work can reuse the same rules without spreading cloud context across multiple files.
- Sprint 2 established the Terraform foundation layer: repository layout, provider/version pinning, remote-state bootstrap, VPC, subnets, routing, NAT, and least-privilege security groups for ALB, ECS, and RDS.
- Sprint 3 now defines the AWS runtime layer as Terraform code for `dev`: ECR, CloudWatch logs, RDS PostgreSQL, Secrets Manager/SSM runtime configuration, ECS Fargate, ALB, target group, listener, health check, and optional future HTTPS/ACM/Route53 wiring.
- Terraform CLI is installed locally and the AWS provider is pinned as `hashicorp/aws` `5.100.0`. The active Terraform target account is now AWS account `661000947340` (`Pablo`, alias/profile `aws-pey-v1`), with expected remote-state bucket `transitops-tfstate-661000947340-eu-west-1` and DynamoDB lock table `transitops-tfstate-locks`. HTTPS/Route53 for `api.dev.transitops.net` is now enabled for future `dev` applies because the hosted zone exists and is delegated in this account.

## MVP Direction

According to the repository documentation, the MVP is intended to include:

- Transport, vehicle, and driver management
- Assignments between transports, vehicles, and drivers
- Transport lifecycle/state transitions
- Shipment/logistics events
- JWT authentication and basic roles
- Basic user bootstrap and admin user management
- PostgreSQL persistence
- Initial tests
- Local Docker-based reproducibility

## Excluded From MVP

These are outside the local functional MVP, but they are central to the overall project delivery and now receive dedicated planning time as soon as the minimum backend surface is stable enough to deploy:

- AWS infrastructure
- Terraform deployment
- ECS/ECR/RDS rollout
- CloudWatch dashboards and alarms
- Full CI/CD deployment automation
- Advanced observability/resilience features
- Frontend

## Architecture Direction

- Backend-first, modular and maintainable
- Minimum local MVP first, then immediate cloud-first implementation and hardening
- Strong emphasis on reproducibility, testing, and operational clarity
- Documentation quality matters because the project is also intended to be defensible in an academic context

## Source Documents

- `README.md`
- `docs/CloudArchitecture.md`
- `docs/CloudReliability.md`
- `docs/CloudOperations.md`
- `docs/RequirementsTraceability.md`
- `docs/FinalEvidence.md`
- `docs/FinalVerification.md`
- `infra/terraform/README.md`
- `docs/Requirements.md`
- `docs/Roadmap.md`

## Working Convention

- `AGENTS.md` contains stable agent instructions and user preferences.
- `CONTEXT.md` should be updated as work progresses and decisions are made.
- If a decision is temporary, uncertain, or likely to change soon, record it here instead of in `AGENTS.md`.

## Recent Decision Log

- 2026-04-25: Started Sprint 4 implementation by adding a dedicated `--migrate-only` API execution mode for EF Core migrations, Terraform-managed GitHub Actions OIDC deployment role support, manual `terraform-dev` and `deploy-dev` GitHub Actions workflows, and `docs/CloudDeployment.md` for the AWS code-to-cloud path.
- 2026-04-25: First Sprint 4 `dev` Terraform apply partially created the AWS foundation/runtime resources but exposed two real environment corrections: the documented Route53 hosted zone ID is not present in account `142966787103`, so the first deployment now supports an HTTP ALB-DNS fallback until DNS is delegated; PostgreSQL was moved from unavailable RDS engine version `16.3` to available version `16.13`.
- 2026-04-25: Completed the Sprint 4 `dev` cloud bootstrap path in AWS account `142966787103`: Terraform created the cost-bearing `dev` foundation/runtime stack, GitHub OIDC role `transitops-dev-github-actions-deploy-role`, ECR repository `transitops/api`, RDS PostgreSQL, runtime Secrets Manager/SSM entries, ALB, and ECS service. Published image tag `sprint4-local-d00aa6b` to ECR, registered ECS task definition revision `2`, ran the one-off ECS migration task `2e6447ee228f4f1594868fabd14ef76c` with exit code `0`, scaled the service to desired/running `1/1`, verified `GET http://transitops-dev-alb-1052623962.eu-west-1.elb.amazonaws.com/api/v1/health/ready` returns `200`, bootstrapped the first admin with status `201`, and verified admin login returns a JWT. DNS/HTTPS remains deferred until the `transitops.net` hosted zone is actually present/delegated in the AWS account.
- 2026-04-25: Finished the Sprint 4 DNS/HTTPS closure for `dev`: created/delegated the real `transitops.net` public hosted zone in the `transitops-dev` account (`Z0675583AI7IBDN9DQWU`), validated ACM certificate `arn:aws:acm:eu-west-1:142966787103:certificate/6a48228f-5631-478b-9a82-3e731edc610c`, converged Terraform with HTTP-to-HTTPS redirect plus HTTPS listener, and verified `GET https://api.dev.transitops.net/api/v1/health/ready` returns `200` and admin login returns a JWT.
- 2026-04-25: Added the university TFG LaTeX template under `memoria-tfg/` and replaced its demo content with a minimal Spanish thesis structure aligned with the signed anteproyecto: cloud transport-management backend on AWS, Terraform/IaC, CI/CD, observability, security, validation, and final conclusions.
- 2026-04-25: Expanded the TFG memory draft with substantive Spanish content across abstract, introduction, objectives, methodology, requirements, architecture, backend implementation, infrastructure/deployment, CI/CD, observability/security, validation/results, conclusions, and evidence appendix. The LaTeX build is configured to use XeLaTeX via `.latexmkrc`, and `latexmk -interaction=nonstopmode memoria_tfg.tex` generates `memoria_tfg.pdf`; remaining work is mainly to add real screenshots/evidence and polish layout/wrapping.
- 2026-03-25: Established repository convention to use `AGENTS.md` for stable agent instructions/preferences and `CONTEXT.md` for evolving project context.
- 2026-03-25: Added initial PostgreSQL schema script under `database/postgres/` to keep database artifacts outside the API project code and aligned with the documented stack.
- 2026-03-26: Updated `database/postgres/001_initial_schema.sql` to use partial unique indexes on active rows (`deleted_at IS NULL`) so business keys can be reused after soft delete.
- 2026-03-26: Hardened `database/postgres/001_initial_schema.sql` with transactional execution and idempotent named-constraint guards so reruns are safer after partial setup attempts.
- 2026-03-26: Added `docker-compose.yml` to run the API and PostgreSQL together locally, with the initial schema mounted into `/docker-entrypoint-initdb.d/` for first-time database initialization.
- 2026-03-26: Replaced the template weather endpoint with versioned transport/vehicle/driver/shipment-event controllers, a common API response envelope, and initial domain contracts inside `TransitOps.Api`.
- 2026-03-26: Simplified the solution to a KISS structure with only `TransitOps.Api` and `TransitOps.Tests`, removing bootstrap service abstractions and keeping only minimal internal folders (`Controllers`, `Contracts`, `Domain`, `Common`, `Middleware`, `Errors`).
- 2026-03-26: Integrated EF Core with PostgreSQL inside `TransitOps.Api/Infrastructure/Persistence`, added `TransitOpsDbContext`, entity configurations, a design-time factory, and the baseline `InitialCreate` migration aligned with the current schema, including partial unique indexes and `set_updated_at` triggers.
- 2026-03-26: Updated `api/v1/health/ready` to check PostgreSQL connectivity through `TransitOpsDbContext.Database.CanConnectAsync()` and return `503 Service Unavailable` when the database is unreachable.
- 2026-03-28: Replaced backlog-based planning with `docs/Requirements.md` as the formal scope and acceptance baseline, and `docs/Roadmap.md` as the daily execution plan, preserving March 24-27 as completed historical baseline.
- 2026-03-28: Expanded `docs/Requirements.md` with explicit user-management requirements, admin/operator permission boundaries, main flows, and stronger functional detail.
- 2026-03-28: Replanned `docs/Roadmap.md` again so each day represents a coherent slice of roughly one hour, incorporates the new user-administration work, and adjusts the target end date to 2026-05-12.
- 2026-03-28: Implemented the March 28 transport read slice: `GET /api/v1/transports` and `GET /api/v1/transports/{id}` now query PostgreSQL through `TransitOpsDbContext`, filter soft-deleted rows, and return real data or `404` as appropriate.
- 2026-03-28: Refactored the transport read slice so `TransportsController` delegates to a transport-specific application service, keeping the controller as a thin HTTP entry point while still using EF Core-backed projection for the transport read model.
- 2026-03-28: Added `TransitOps.Api/TransitOps.Api.postman_collection.json` with all current API endpoints for manual testing, including placeholders and the implemented health/transport reads.
- 2026-03-28: Added integration tests for the implemented health and transport read endpoints using `WebApplicationFactory` and an in-memory EF Core test database.
- 2026-03-28: Moved PostgreSQL sample-data scripts to the repository `scripts/` area and retired `database/` as a schema source so `TransitOps.Api/Infrastructure/Persistence/Migrations` is the only canonical definition of the database schema.
- 2026-03-28: Updated `docker-compose.yml` and API startup so the local Docker flow uses a fresh PostgreSQL volume and applies EF Core migrations on startup when `Database:ApplyMigrationsOnStartup` is enabled.
- 2026-03-28: Moved `Persistence` under `Infrastructure` so EF Core persistence, migrations, and database-facing queries live under the same infrastructure layer.
- 2026-03-29: Implemented the March 29 transport write slice: `POST /api/v1/transports` and `PUT /api/v1/transports/{id}` now persist through `TransitOpsDbContext`, keep new transports in `planned`, validate planned-date ordering, reject duplicate active references with `409 Conflict`, and return `404` for missing or deleted transports.
- 2026-03-29: Added transport write integration tests covering successful create/update plus validation, not-found, and duplicate-reference scenarios, and updated the HTTP/Postman request artifacts to include the new transport write endpoints.
- 2026-03-29: Fixed the remaining Docker API startup error by installing `libgssapi-krb5-2` in the runtime image; this removes the Linux shared-library warning from Npgsql during container startup while keeping the API healthy against PostgreSQL.
- 2026-03-30: Removed the two empty enum-alignment migrations (`SyncEnumMappings` and `AlignPublicEnumModel`) plus their designers, leaving only the baseline migration and the real enum column-type qualification migration in the repository.
- 2026-03-30: Confirmed that with the current `Npgsql.EntityFrameworkCore.PostgreSQL` `10.0.1` provider, EF Core still sends `Transport.Status` as an integer on PostgreSQL inserts despite the enum registrations; transport creation therefore intentionally keeps the targeted SQL insert workaround for `status` until the provider/runtime mapping issue is resolved with a validated alternative.
- 2026-03-30: Replaced native PostgreSQL enums with `smallint` columns plus explicit check constraints for `transport.status`, `shipment_event.event_type`, and `app_user.user_role`; the new migration preserves existing rows with `USING CASE`, drops the old enum types after conversion, removes the transport SQL insert workaround, and leaves EF Core using its normal enum-to-number mapping.
- 2026-04-02: Removed the artificial `Application/Commands` vs `Application/Queries` split for the implemented transport slice, consolidating it into a single `ITransportService` and `TransportService` so the current CRUD-oriented codebase is easier to navigate and maintain.
- 2026-04-02: Implemented full vehicle and driver CRUD on top of `TransitOpsDbContext`, plus transport list filters by status/date/vehicle/driver and basic pagination metadata, and added integration tests covering those new slices.
- 2026-04-02: Implemented the explicit transport assignment flow on planned transports, requiring an active, non-deleted vehicle and driver together, and added integration tests for successful assignment, reassignment, validation, inactive resources, and missing-resource cases.
- 2026-04-02: Implemented explicit transport lifecycle transitions with terminal-state validation, assignment prerequisite checks for `planned -> in_transit`, timestamp capture for actual pickup/delivery, and integration coverage for valid and invalid state changes.
- 2026-04-02: Implemented shipment-event creation and chronological history on `POST/GET /api/v1/transports/{transportId}/shipment-events`, validating the target transport and explicit actor user and returning actor traceability data in the shipment-event response before the later JWT-authentication slice moved actor resolution to the authenticated caller context.
- 2026-04-02: Fixed the shipment-event history query after the first live Docker smoke run exposed an EF Core translation failure on PostgreSQL; chronological ordering now happens on the entity query before DTO projection, and the end-to-end smoke flow passes while still leaving zero rows behind after cleanup.
- 2026-04-02: Added `002_seed_sample_data.bat` and `003_delete_seed_sample_data.bat` alongside the manual seed SQL so the deterministic sample dataset can be executed directly against the local Docker `db` service without requiring a separate local `psql` installation.
- 2026-04-02: Added a runner-safe Postman/Newman smoke test flow under the repository `scripts/` area, including a local environment file, a collection that mixes deterministic seeded reads with runtime-generated write data, and `run_local_api_smoke.bat` to start Docker, reset seed data, and execute the whole live API check in one command.
- 2026-04-02: Reorganized `scripts/` by purpose, moving seed/reset assets to `scripts/database/postgres/seed/` and the live API smoke assets to `scripts/testing/postman/`, then updated the batch runners and repository documentation to follow the new layout.
- 2026-04-02: Hardened `scripts/testing/postman/run_local_api_smoke.bat` so the smoke flow now deletes any runtime-generated transports, vehicles, and drivers with direct SQL cleanup before exit, including failure cases, leaving no smoke-test rows behind in the local database.
- 2026-04-02: Hardened `scripts/testing/postman/run_local_api_smoke.bat` again so the smoke flow also removes the deterministic seed dataset on exit; it is now intended to leave no generated rows behind at all.
- 2026-04-03: Implemented `POST /api/v1/auth/bootstrap-admin` and `POST /api/v1/auth/login`, using hashed passwords plus JWT issuance from externalized configuration, applied bearer protection to business controllers, switched shipment-event actor traceability from client-supplied `createdByUserId` to the authenticated caller, updated the deterministic seed users to valid loginable hashes, and extended the live Docker smoke flow to authenticate before exercising protected endpoints.
- 2026-04-03: Implemented admin-only user-management on `GET /api/v1/users`, `GET /api/v1/users/{id}`, `POST /api/v1/users`, `PUT /api/v1/users/{id}/role`, and `PUT /api/v1/users/{id}/activation`, hashing created passwords, restricting access to admins only, and preventing the last active admin from being deactivated or demoted.
- 2026-04-03: Stabilized the local MVP execution baseline by adding `.env.example`, making `docker compose` fail early when the JWT signing key is missing while treating the bootstrap token as optional, documenting the full local verification path in `docs/LocalVerification.md`, adding a ready-to-import local Postman environment for the manual collection, adding a GitHub Actions `restore/build/test + migration drift + compose config` workflow, and aligning `TransitOps.Api.csproj` to `Microsoft.EntityFrameworkCore.Relational` `10.0.5` so the previous EF Core version-conflict warning disappears from the test baseline.
- 2026-04-06: Added the initial cloud-design baseline before Terraform work, fixing the target AWS topology to Route53 + ACM + ALB + ECS Fargate + RDS PostgreSQL + ECR + CloudWatch, defining private networking and OIDC-based delivery as the intended model, and standardizing naming, tagging, environment, DNS, and runtime configuration conventions for all upcoming cloud artifacts.
- 2026-04-07: Aligned the main manual Postman collection, manual Postman environment, HTTP file, and local verification guide so authentication is no longer ambiguous: a fresh local stack now uses `bootstrap-admin` plus login with the bootstrapped admin credentials, while login with deterministic seeded admin credentials remains an explicit alternative path only when the manual sample seed dataset has been applied.
- 2026-04-07: Added the first Terraform foundation slice under `infra/terraform/`, creating a `modules/platform_foundation` shared-convention module plus `environments/dev` and `environments/prod` roots with provider/version pinning, shared locals/variables/outputs, example backend configuration files, and Terraform-specific git-ignore rules. No remote state or AWS resources exist yet.
- 2026-04-07: Added `infra/terraform/bootstrap/remote_state/` to implement the remote Terraform state strategy: the bootstrap root now defines an encrypted/versioned S3 bucket with public access blocked, a server-side-encrypted DynamoDB lock table, concrete backend outputs for `dev` and `prod`, and the environment roots now include partial `backend "s3" {}` declarations so they can be reinitialized against the shared backend after bootstrap.
- 2026-04-11: Consolidated AWS documentation into `docs/CloudArchitecture.md`, merging the target topology, cloud conventions, and Terraform remote-state bootstrap path into one compact cloud reference and removing the previous split across three separate documents.
- 2026-04-11: Installed/used local Terraform `1.14.8`, ran `terraform fmt -recursive infra/terraform`, initialized the bootstrap/dev/prod Terraform roots with `-backend=false`, and confirmed `terraform validate` succeeds for all three roots before any real AWS backend or networking apply.
- 2026-04-19: Implemented the Sprint 3 AWS runtime layer in Terraform without applying AWS resources: added reusable modules for ECR, CloudWatch logs, RDS PostgreSQL, Secrets Manager/SSM runtime configuration, and ECS Fargate/ALB runtime; wired them into `environments/dev`; kept `prod` structurally compatible; and documented the temporary HTTP-only `dev` posture until a real Route53/ACM domain exists.
- 2026-04-19: Ran `terraform fmt -recursive infra/terraform`, initialized `bootstrap/remote_state`, `environments/dev`, and `environments/prod` with `-backend=false`, and confirmed `terraform validate` succeeds for all three roots after the Sprint 3 runtime changes.
- 2026-04-20: Created an isolated AWS Organizations member account for TransitOps (`transitops-dev`, account `142966787103`), enabled IAM Identity Center in `eu-west-1`, assigned the local user `Pey` `AdministratorAccess` to that account, configured AWS CLI SSO profile `transitops-dev`, and confirmed local CLI identity points to the isolated account.
- 2026-04-20: Applied the Terraform remote-state bootstrap in the isolated `transitops-dev` account, creating the S3 state bucket `transitops-tfstate-142966787103-eu-west-1` and DynamoDB lock table `transitops-tfstate-locks`; configured `environments/dev/backend.hcl` locally; reinitialized `dev` against the remote backend; created ignored local `terraform.tfvars` for planning; and confirmed `terraform plan` succeeds for the `dev` foundation/runtime stack with 51 resources to add and HTTP ingress.
- 2026-04-20: Registered/created Route53 DNS foundation for `transitops.net`, captured hosted zone ID `Z0844787W37HXN9FJR`, enabled HTTPS in the ignored local `dev` Terraform variables, and confirmed a real AWS `terraform plan` succeeds with ACM certificate validation, Route53 DNS records, HTTP-to-HTTPS redirect, HTTPS listener, and target hostname `api.dev.transitops.net`.
- 2026-04-26: Destroyed the cost-bearing `dev` Terraform runtime in AWS to keep the academic/practice project affordable after Sprint 4 evidence capture. Verified Terraform state is empty and that RDS, ALB, NAT Gateway, and ECS clusters are no longer present in `eu-west-1`; remote state/bootstrap resources and Route53/domain ownership may still remain as low-cost/reusable foundation.
- 2026-04-26: Retargeted Terraform/cloud configuration from the old `transitops-dev` account (`142966787103`) to AWS account `661000947340` (`Pablo`, alias/profile `aws-pey-v1`). Updated the dev account ID, expected state bucket name, local backend/profile settings, deployment documentation, and added stable cleanup tags `TerraformStack` and `ResourceGroup` through provider `default_tags` plus explicit shared tag maps so AWS Resource Explorer/Resource Groups can filter `tag:TerraformStack = transitops-dev` after `terraform destroy`.
- 2026-04-26: Disabled automated RDS backup retention for the disposable educational `dev` environment (`database_backup_retention_days = 0`) while keeping `database_skip_final_snapshot = true`, so future create/destroy cycles do not leave cost-bearing automated snapshots by default.
- 2026-05-01: Completed Sprint 6 observability/deployment-hardening implementation and AWS `dev` validation in account `661000947340`: added API request correlation via `X-Correlation-ID`, JSON console logging for CloudWatch, Terraform-managed CloudWatch dashboard/alarms/SNS email notification support, ECS deployment circuit breaker rollback, explicit ALB/ECS health/deployment tuning variables, and GitHub Actions checks for observability resources and correlated log output. Recreated the cost-bearing `dev` stack, restored/imported Secrets Manager secrets that were pending deletion from the previous destroy, published image `sprint6-local-6bb910c` to ECR, ran migration task `arn:aws:ecs:eu-west-1:661000947340:task/transitops-dev-cluster/345ae53d1340475aa94a826328404998` with exit code `0`, scaled ECS service `transitops-dev-api-svc` to desired/running `1/1`, verified ALB readiness at `http://transitops-dev-alb-386295539.eu-west-1.elb.amazonaws.com/api/v1/health/ready` with `200`, bootstrapped admin with `201`, verified login with `200`, confirmed dashboard `transitops-dev-api`, log group `/aws/ecs/transitops/dev/api`, 9 CloudWatch alarms, and correlated logs including `sprint6-login-final`. SNS alarm topic was not created because `alarm_email`/GitHub `ALARM_EMAIL` was empty.
- 2026-05-02: Corrected DNS/HTTPS setup for the current AWS account `661000947340`: the domain `transitops.net` and hosted zone are present in this account, with hosted zone ID `Z0844787W37HXN9FIJR`; the registered domain nameservers initially pointed to a different delegation set, so ACM stayed in `PENDING_VALIDATION` despite Terraform-created CNAME records. Updated the registered domain nameservers to match the hosted zone, confirmed public DNS resolution for the ACM validation CNAME, and observed the ACM certificate for `api.dev.transitops.net` reach `ISSUED` before cleanup. Local `infra/terraform/environments/dev/terraform.tfvars` now has `root_domain = "transitops.net"`, `enable_https = true`, and `hosted_zone_id = "Z0844787W37HXN9FIJR"` for future local applies; the file remains git-ignored.
- 2026-05-02: Destroyed the cost-bearing Sprint 6 `dev` environment in account `661000947340` with `terraform destroy`. The first destroy removed the expensive resources but stopped on non-empty ECR; deleted the remaining ECR image digests and reran destroy successfully. Verified Terraform state is empty and no `transitops-dev` ALB, RDS, ECS cluster, ECR repository, ACM certificate, `api.dev.transitops.net` Route53 records, CloudWatch log group/dashboard/alarms, or runtime secrets remain. NAT Gateway is deleted. Remaining resources intentionally outside this destroy are the registered domain `transitops.net`, the public hosted zone, and the remote Terraform backend S3/DynamoDB resources.
- 2026-05-02: Updated the TFG memory draft under `memoria-tfg/` to reflect Sprint 6: request correlation, JSON CloudWatch logs, dashboard, alarms, SNS email optionality, ECS deployment hardening, DNS/ACM troubleshooting in the current AWS account, Sprint 6 validation evidence, and final `terraform destroy`/cost cleanup. Added descriptive LaTeX placeholders for future screenshots instead of referencing missing image files.
- 2026-05-09: Completed Sprint 7 reliability implementation and real AWS validation/destroy on top of the Sprint 6 baseline. Added Docker context hardening through `.dockerignore`, removed unused port `8081` from the API Dockerfile, added ECS container `stopTimeout`, enabled dev-only ECR `force_delete`, updated cloud connection strings with explicit Npgsql timeout/pooling settings, added manual rollback workflow `.github/workflows/rollback-dev.yml`, and added real RDS restore validation script `scripts/cloud/aws/Invoke-RdsRestoreTest.ps1`. Local validation passed: 87 .NET tests, Terraform fmt/validate, Docker build, and non-root runtime user check. Recreated AWS `dev` in account `661000947340`, published image `sprint7-good-3dca035`, ran migration task `arn:aws:ecs:eu-west-1:661000947340:task/transitops-dev-cluster/1ec56ac77fbb4142a380b9e4881e3d24` with exit code `0`, scaled ECS to desired/running `1/1`, verified HTTPS readiness `200`, bootstrap `201`, login `200`, CloudWatch JSON logs with correlation ids, dashboard `transitops-dev-api`, 9 alarms, and SNS email subscription for `pablomlopez03@gmail.com` in `PendingConfirmation`. Tested bad-image rollback with missing tag `sprint7-missing-3dca035`, failed task `cc06e30b1b7e42eab5aaae55e4155427` (`CannotPullContainerError`), then recovered with the good tag and readiness `200`. Tested RDS restore through manual snapshot, temporary restored DB, temporary secret, temporary task definition, and ECS `--migrate-only` task `7f53d7309acf42349137213deca05080` with exit code `0`; the restore script now grants/removes temporary ECS execution-role permission for the temporary secret. Destroyed the `dev` stack at the end (`72 destroyed`), verified Terraform state empty and no ALB, ECS, RDS, ECR, ACM cert, `api.dev` records, CloudWatch logs/dashboard/alarms, runtime secrets, or temporary snapshots remained. The domain, hosted zone, and Terraform remote backend remain intentionally alive.
- 2026-05-09: Completed Sprint 8 security/cost/recreate/runbooks implementation and real AWS validation/destroy. Added `docs/CloudOperations.md` with security review, cost review, recreate-from-scratch path, and runbooks for deploy, rollback, ECS restart, bootstrap-admin, correlation-id log lookup, CloudWatch alarm response, RDS restore, and destroy/audit. Added `scripts/cloud/aws/Test-Sprint8AwsPosture.ps1` and `scripts/cloud/aws/Test-Sprint8DestroyAudit.ps1`. Updated cloud docs, roadmap, `terraform.tfvars.example`, and TFG memory text/placeholders. Local validation passed: 87 .NET tests, Terraform fmt/validate, Docker build, and non-root runtime user check. Recreated AWS `dev` from empty state with `ecs_desired_count=0`, created 72 resources, published image `sprint8-good-fdf98b9` (`sha256:39fb9a78726912414acbdb1af2f27e59c4ea8552b0a023a4ceab51ef74fdd6bd`), updated runtime secrets, ran migration task `arn:aws:ecs:eu-west-1:661000947340:task/transitops-dev-cluster/cc374ade9c144745bd7566fb9289ffe3` with exit code `0`, scaled ECS to desired/running `1/1` on task definition `transitops-dev-api:9`, verified HTTPS readiness `200` with `sprint8-health`, bootstrap `201` with `sprint8-bootstrap`, login `200` with `sprint8-login`, dashboard/log group/9 alarms/SNS topic and email subscription in `PendingConfirmation`, and `terraform plan -detailed-exitcode` with no changes. AWS posture audit passed: RDS private, ECS no public IP, expected security groups, ECS task role without inline policies, execution role runtime config without `Resource=*`, and GitHub OIDC Terraform deploy role broad permissions documented as accepted `dev` tradeoff. Destroyed `dev` (`72 destroyed`), forced immediate deletion of runtime secrets after Terraform scheduled deletion, verified state empty and destroy audit passed with no ECS, RDS, ECR, ALB, ACM, `api.dev` records, CloudWatch resources, runtime secrets, or manual dev snapshots remaining. Domain, hosted zone, and remote Terraform backend remain intentionally alive.
- 2026-05-22: Implemented Sprint 9 final documentation closure without adding API endpoints or business functionality. Added `docs/RequirementsTraceability.md`, `docs/FinalEvidence.md`, and `docs/FinalVerification.md`; updated `docs/Requirements.md` so delivered functional and non-functional requirements no longer show obsolete `Partial`/`Pending` statuses; updated `README.md`, `docs/Roadmap.md`, `infra/terraform/README.md`, and the TFG memory to describe the real post-Sprint 8/9 state. Local validation passed for `dotnet test` (87 tests), `terraform fmt -check`, `terraform validate`, and LaTeX PDF build. AWS read-only validation confirmed account `661000947340`, empty Terraform state, and `Test-Sprint8DestroyAudit.ps1` passed with no cost-bearing `transitops-dev` runtime resources. Docker build was blocked because Docker Desktop was not running. Sprint 9 treats the Sprint 8 AWS validation as the latest full cloud evidence and keeps any fresh AWS recreate optional for final screenshots; if executed, the final verification guide requires immediate `terraform destroy` and destroy audit.
- 2026-04-01: Replanned `docs/Roadmap.md` through 2026-05-31 so the backend functional surface is closed quickly and the majority of remaining time is explicitly allocated to Terraform, AWS deployment, CI/CD, observability, security, runbooks, evidence capture, and final technical defense material.
- 2026-04-02: Refined `docs/Roadmap.md` again so each day has a more even time budget, a deterministic primary artifact, a mandatory verification step, and explicit coverage of cloud-engineering concerns such as remote Terraform state, tagging, Route53/ACM, ECR scanning, GitHub OIDC, rollback, and backup/restore.
- 2026-04-02: Reworked `docs/Roadmap.md` from daily planning into weekly sprints starting on 2026-03-30, each with a dominant phase, bounded scope, deliverables, and exit criteria, because sprint-level planning better fits the project than day-level task slicing.
- 2026-04-02: Reworked `docs/Roadmap.md` again into three equal-duration sprints of three weeks each, compressing the local-MVP work into Sprint 1 and making Sprint 2 and Sprint 3 explicitly cloud-heavy with stronger done criteria for platform, delivery, observability, rollback, restore, security, and evidence.
- 2026-04-02: Reworked `docs/Roadmap.md` again into one-week sprints, explicitly compressing the whole March 30 to April 12 local-MVP block into Sprint 1 because that scope is considered feasible in one week, leaving nearly all remaining sprints cloud-heavy.

## Open Notes

- Persistence is now wired at infrastructure level. Transport, vehicle, and driver list/detail/create/update/logical delete are database-backed, transport listing supports the demo filters/pagination requested in `docs/Requirements.md`, transport assignment plus lifecycle transitions are available as explicit flows, shipment events now support create/history with actor traceability from the authenticated caller, first-admin bootstrap plus JWT login are available, and admin-only user-management now covers list/detail/create/role-change/activation flows with last-active-admin protection.
- Legacy local PostgreSQL volumes created from the retired SQL-bootstrap flow should be reset before using the new migrations-managed Docker startup.
- `GET /api/v1/health/ready` already confirms whether the API can connect to PostgreSQL in the current environment.
- Manual sample-data scripts under `scripts/database/postgres/seed/` are now aligned with the `smallint` enum mapping strategy used by the live schema.
- The most reliable "all endpoints against real data" local verification path is now `scripts/testing/postman/run_local_api_smoke.bat`; it uses the live Docker API plus PostgreSQL, deterministic seed reset, a runner-safe Postman collection, and cleanup of both runtime and seed rows so no generated smoke data remains after execution.
- The manual verification path is now intentionally standardized: `.env.example` -> `docker compose up --build` -> login through `.http` or the Postman collection using `scripts/testing/postman/environments/TransitOps.Api.manual.local.postman_environment.json`.
- The manual verification artifacts now distinguish two valid auth paths instead of mixing them: `bootstrap-admin` + login for a fresh stack, or `Login With Seeded Admin` only after the manual sample seed dataset has been applied.
- The CI baseline is now `.NET restore/build/test`, EF Core migration drift check through `dotnet-ef migrations has-pending-model-changes`, and `docker compose config` validation.
- The cloud-design baseline is now intentionally explicit and centralized: use `docs/CloudArchitecture.md` for the target topology, names, tags, environment boundaries, DNS patterns, configuration/secrets rules, and Terraform remote-state bootstrap path.
- The Terraform code now has two main deployable layers: `platform_foundation` for VPC/subnets/routes/NAT/security groups and the Sprint 3 runtime modules for ECR, logs, RDS, runtime config, ECS, and ALB.
- The remote-state bootstrap path is now explicit: run `infra/terraform/bootstrap/remote_state/` first with local state, then feed its outputs into `infra/terraform/environments/{dev,prod}/backend.hcl` and re-run `terraform init -backend-config=backend.hcl` in each environment root.
- The `dev` runtime targets account `661000947340`. `transitops.net` is present in this account, hosted zone `Z0844787W37HXN9FIJR`, and DNS/ACM is now configured for future applies with `enable_https = true`; re-applying will recreate resources such as NAT Gateway, ALB, RDS, ECS, ECR, runtime secrets, CloudWatch logs/dashboard/alarms, ACM certificate, and the `api.dev.transitops.net` Route53 records.
- Terraform-managed resources now carry `TerraformStack` and `ResourceGroup` tags. Use `tag:TerraformStack = transitops-dev` to audit environment resources, and `tag:TerraformStack = transitops-bootstrap-remote-state` for the remote-state bootstrap resources that intentionally survive environment destroys.
- The `dev` database intentionally uses `database_backup_retention_days = 0` and skips final snapshots to minimize residual AWS cost for educational runs. Existing snapshots created before this change must be deleted separately if no longer needed.
- Sprint 6 observability now expects the API to return `X-Correlation-ID` on every response and to emit JSON request logs with `State.CorrelationId`; Terraform creates dashboard `transitops-dev-api`, alarms for ALB/ECS/RDS/application errors, and optional SNS email notifications when `alarm_email`/GitHub `ALARM_EMAIL` is configured. The Sprint 6 live validation used the ALB HTTP fallback first; DNS/ACM was then corrected for the current account, and the cost-bearing environment was destroyed after evidence capture.
- Sprint 7 reliability is implemented and validated: rollback is a Terraform reapply to a known-good ECR tag through `.github/workflows/rollback-dev.yml`; controlled failure used missing tag `sprint7-missing-3dca035` and recovered to `sprint7-good-3dca035`; RDS restore used a manual snapshot, temporary restored DB, temporary Secrets Manager connection string, temporary task definition, temporary execution-role permission for that secret, and an ECS `--migrate-only` task with exit code `0`, followed by cleanup.
- Sprint 8 security/cost/operations is implemented and validated: `docs/CloudOperations.md` is the current runbook entrypoint, `Test-Sprint8AwsPosture.ps1` verifies the running stack, and `Test-Sprint8DestroyAudit.ps1` verifies post-destroy cleanup.
- Sprint 9 final closure is implemented: `docs/RequirementsTraceability.md` maps delivered FR/NFR to implementation, verification, and evidence; `docs/FinalEvidence.md` lists evidence already captured plus exact screenshot placeholders; `docs/FinalVerification.md` defines local final checks, optional AWS recreate/destroy, and defense rehearsal order.
- Local Terraform files that may contain environment-specific or sensitive values (`terraform.tfvars`, `backend.hcl`, `.terraform/`) are ignored by Git. The current local `dev` variable file is configured for future recreations with `enable_https = true`, hosted zone `Z0844787W37HXN9FIJR`, `alarm_email = "pablomlopez03@gmail.com"`, `ecs_desired_count = 0` for the first recreate, and `ecr_force_delete = true`; any secret values in that file are local-only and must not be committed. The latest Sprint 8 run destroyed the cost-bearing environment after validation.
- `.terraform.lock.hcl` files generated by `terraform init` should be committed for each Terraform root so provider resolution remains reproducible.
- Roadmap entries through 2026-03-29 should be treated as completed history. From 2026-03-30 onward, the actionable plan now runs as one-week sprints through 2026-05-31 with explicit cloud-first prioritization.
- Future sessions should update this file when meaningful project decisions, architecture changes, or scope adjustments are made.
