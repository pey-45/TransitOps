# This file is intentionally an example because the real backend resources
# do not exist yet. The next Sprint 2 slice will create the remote-state
# bootstrap path that can satisfy these placeholders.

bucket         = "transitops-tfstate-<account-id>-eu-west-1"
key            = "prod/foundation.tfstate"
region         = "eu-west-1"
dynamodb_table = "transitops-tfstate-locks"
encrypt        = true
